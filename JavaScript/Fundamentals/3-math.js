// Utilizadades do objeto Math:
console.log(typeof Math);

const number = 2;
const pi = Math.PI;

console.log("Valor de Número: ", number);
console.log("Valor de PI: ", pi);

// Exponenciação:
// Ao quadrado:
console.log(Math.pow(number, 2));

// Ao cubo:
console.log(Math.pow(number, 3));

console.log(Math.random(1, 10))